To build the javascript for the site:
* run `npm install` at the root directory of this project to install dependencies
* then run `npm run build` also in the root directory to compile the project
* minified and bundled js file will be put in `/dist` folder as `main.js`
